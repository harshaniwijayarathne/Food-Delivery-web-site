export interface Issue {
    path: string[];
    message: string;
    code: string;
}
export interface FormattedError {
    message: string;
    errors: Record<string, string[]>;
}
export declare class ValidationError extends Error {
    issues: Issue[];
    constructor(issues: Issue[]);
    private static buildMessage;
    /**
     * Format issues as an array, optionally applying locale messages
     * @param localeMessages - Optional mapping of error codes to localized messages
     */
    format(localeMessages?: Record<string, string>): {
        path: string[];
        message: string;
        code: string;
    }[];
    /**
     * Format errors grouped by field path
     */
    formatErrors(): FormattedError;
    /**
     * @deprecated Use format(localeMessages) instead
     */
    formatWithLocale(localeMessages: Record<string, string>): {
        path: string[];
        message: string;
        code: string;
    }[];
}
//# sourceMappingURL=ValidationError.d.ts.map