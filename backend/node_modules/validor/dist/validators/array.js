"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.array = array;
const ArraySchema_1 = require("../schemas/ArraySchema");
/**
 * Creates a new ArraySchema for validating array values
 * @param itemSchema - Optional schema to validate each array item
 * @returns A new ArraySchema instance
 *
 * @example
 * // Array of any items
 * const schema = array();
 *
 * @example
 * // Array of strings
 * const schema = array(string().min(3));
 *
 * @example
 * // Array with length constraints
 * const schema = array(number()).min(1).max(10);
 */
function array(itemSchema) {
    return new ArraySchema_1.ArraySchema(itemSchema);
}
//# sourceMappingURL=array.js.map