import { ArraySchema } from "../schemas/ArraySchema";
import { BaseSchema } from "../core/BaseSchema";
/**
 * Creates a new ArraySchema for validating array values
 * @param itemSchema - Optional schema to validate each array item
 * @returns A new ArraySchema instance
 *
 * @example
 * // Array of any items
 * const schema = array();
 *
 * @example
 * // Array of strings
 * const schema = array(string().min(3));
 *
 * @example
 * // Array with length constraints
 * const schema = array(number()).min(1).max(10);
 */
export declare function array<T = any>(itemSchema?: BaseSchema<T>): ArraySchema<T>;
//# sourceMappingURL=array.d.ts.map