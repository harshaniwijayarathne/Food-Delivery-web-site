import { ObjectSchema } from '../schemas/ObjectSchema';
import { BaseSchema } from '../core/BaseSchema';
type SchemaDefinition = Record<string, BaseSchema<any>>;
/**
 * Creates an object schema validator
 *
 * @example
 * ```ts
 * const schema = object({
 *   name: string().required(),
 *   age: number().required(),
 * });
 * const result = await schema.validate({ name: 'John', age: 30 });
 * ```
 */
export declare function object<T extends SchemaDefinition>(shape: T): ObjectSchema<T>;
export {};
//# sourceMappingURL=object.d.ts.map