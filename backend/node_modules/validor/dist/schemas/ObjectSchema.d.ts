import { BaseSchema } from "../core/BaseSchema";
type SchemaDefinition = Record<string, BaseSchema<any>>;
type InferShape<T extends SchemaDefinition> = {
    [K in keyof T]: T[K] extends BaseSchema<infer U> ? U : never;
};
export declare class ObjectSchema<T extends SchemaDefinition> extends BaseSchema<InferShape<T>> {
    private shape;
    private strictMode;
    constructor(shape: T);
    parse(value: unknown, path?: string[]): InferShape<T>;
    validate(value: unknown, path?: string[]): Promise<InferShape<T>>;
    private checkValue;
    private validateFieldsSync;
    private validateFieldsAsync;
    private collectError;
    strict(): this;
    passthrough(): this;
    extend<U extends SchemaDefinition>(shape: U): ObjectSchema<T & U>;
    pick<K extends keyof T>(...keys: K[]): ObjectSchema<Pick<T, K>>;
    omit<K extends keyof T>(...keys: K[]): ObjectSchema<Omit<T, K>>;
    partial(): ObjectSchema<{
        [K in keyof T]: BaseSchema<InferShape<T>[K] | undefined>;
    }>;
    merge<U extends SchemaDefinition>(other: ObjectSchema<U>): ObjectSchema<T & U>;
}
export {};
//# sourceMappingURL=ObjectSchema.d.ts.map